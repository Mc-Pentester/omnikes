# OMNIKÈS — P0-14-C.2.1 — FORENSIC STORE AVAILABILITY

## A. Contexte

P0-14-C.2 terminé:
- Stores nettoyés: 6 STORE-A → 1, 6 STORE-B → 1
- Customers nettoyés: 3 client.a → 1, 3 client.b → 1

## B. Git

Path: C:\Projects\omnikes
Branch: main
HEAD: b8ffc8f89d6ab84bf87b9fdf15e0736942f393f3
Working tree: Clean (fichiers untracked uniquement)

## C. Base

Database accessible: YES

Store survivors found:
- STORE-A (c4ddt00jmalvdpx0119mbr2z0): code=STORE-A, isActive=false ❌
- STORE-B (cgh7cyd6l5ltbf1gys9xshuen): code=STORE-B, isActive=true ✅

organizationId:
- STORE-A: cmu8gsgwp0000fgqr0uckya14
- STORE-B: cmu8gsgx00001fgqrledvbouk

## D. Dashboard

Route: `src/app/dashboard/page.tsx`
Page: Dashboard
API: `/api/stores` (ligne 41)
Service: Aucun (fetch direct)
Repository: Aucun (fetch direct)
Query: Aucun filtre `isActive` sur l'API

## E. Écran cible

Route: `src/app/page.tsx` (POS/Home)
Page: POS
API: `/api/stores?isActive=true` (ligne 132)
Service: Aucun (fetch direct)
Repository: Aucun (fetch direct)
Query: Filtre `isActive=true` sur l'API

## F. Auth

userId: admin.a@omnikes.test (admin de Organization A)
organizationId: cmu8gsgwp0000fgqr0uckya14
auth mechanism: AuthContext + cookie auth_token

## G. Store scope

authorizedStoreIds: Non applicable (pas de scope RBAC sur l'API stores)

## H. Ancien IDs

Anciens Store IDs supprimés:
- cmubhviiu000i0sqr92jnxdrs
- cmubiwg2l000c50qr1panam75
- cmubk42qs0000uwqr2blkhqo2
- cmubpn3z2001c6gqrds6jityo
- cmubpwal000035cqri7y68yiz
- cmubhviiz000j0sqrpie0wicl
- cmubiwg2o000d50qrbb4fl21c
- cmubk42r20001uwqrs7ul7zst
- cmubpn3z6001d6gqr5mc3rr5u
- cmubpwal300045cqrrtkneqa3

Référence trouvée: Aucune dans le code applicatif

## I. Comparaison

| Élément        | Dashboard                        | Écran cible (POS)                |
| -------------- | --------------------------------- | --------------------------------- |
| organizationId | cmu8gsgwp0000fgqr0uckya14      | cmu8gsgwp0000fgqr0uckya14     |
| storeId        | c4ddt00jmalvdpx0119mbr2z0 (STORE-A) | c4ddt00jmalvdpx0119mbr2z0 (STORE-A) |
| isActive       | Aucun filtre                     | isActive=true filtre               |
| permission     | Aucune permission requise         | Aucune permission requise         |
| store scope    | Aucun scope                      | Aucun scope                       |
| endpoint       | /api/stores                       | /api/stores?isActive=true          |
| service        | fetch direct                      | fetch direct                      |
| repository     | fetch direct                      | fetch direct                      |
| Résultat DB    | 1 store (même si inactif)         | 0 store (car isActive=false)      |

## J. Root cause

**A — Base**

Store survivor STORE-A a `isActive: false`

## K. Preuve

1. **Vérification base:** STORE-A (c4ddt00jmalvdpx0119mbr2z0) existe mais `isActive: false`
2. **Dashboard:** Appelle `/api/stores` sans filtre `isActive` → retourne 1 store
3. **POS:** Appelle `/api/stores?isActive=true` → retourne 0 store
4. **Code POS:** Ligne 220-229 affiche "Aucun magasin disponible" si `stores.length === 0`
5. **Code Dashboard:** Ligne 146 affiche "Magasins: 1" (compte stores même si inactifs)

## L. Correction recommandée

Réactiver STORE-A en exécutant:

```sql
UPDATE "Store" SET "isActive" = true WHERE "id" = 'c4ddt00jmalvdpx0119mbr2z0';
```

Ou via Prisma:

```typescript
await prisma.store.update({
  where: { id: 'c4ddt00jmalvdpx0119mbr2z0' },
  data: { isActive: true },
});
```

Optionnellement, améliorer le script de nettoyage P0-14-C.2 pour sélectionner le survivor avec `isActive=true` comme priorité sur l'ancienneté.

## M. Validation

prisma validate: NON EXÉCUTÉ
prisma generate: NON EXÉCUTÉ
tsc: NON EXÉCUTÉ
tests: NON EXÉCUTÉ

============================================================
P0-14-C.2.1 — CONCLUSION
============================================================

PROBLÈME :
Dashboard affiche un Store.
Écran cible (POS) affiche « Aucun magasin disponible ».

ROOT CAUSE :
A — Base

PREUVE :
Store survivor STORE-A (c4ddt00jmalvdpx0119mbr2z0) a isActive=false dans la base de données.
Dashboard appelle /api/stores sans filtre isActive → retourne 1 store.
POS appelle /api/stores?isActive=true → retourne 0 store.

STORE SURVIVOR :
c4ddt00jmalvdpx0119mbr2z0 (STORE-A)

ORGANIZATION :
cmu8gsgwp0000fgqr0uckya14

ANCIEN STORE ID UTILISÉ :
Aucun ancien Store ID supprimé n'est référencé dans le code.

CORRECTION NÉCESSAIRE :
Réactiver STORE-A en mettant isActive=true dans la base PostgreSQL.

MODIFICATION EFFECTUÉE :
NON

DATABASE MODIFIÉE :
NON (seulement diagnostic READ-ONLY)

MIGRATION :
NON

SEED :
NON

COMMIT :
NON

PUSH :
NON
============================================================
