# OMNIKÈS — P0-14-B.0.1 — RAPPORT DÉCONNEXION

## 1. Git

Path: C:\Projects\omnikes
Branch: main
HEAD: 056f5b4 P0-14-A.2: Apply RBAC permissions and store isolation to historical domains

## 2. Mécanisme logout existant

**Route:** POST /api/auth/logout
**Fichier:** src/app/api/auth/logout/route.ts
**Méthode:**
- Récupère le token via getSessionToken()
- Appelle authService.logout(token)
- Supprime le cookie auth_token
- Retourne success: true

**Comportement:**
- Supprime la session côté serveur
- Supprime le cookie auth_token
- AuthContext.logout() (src/contexts/AuthContext.tsx) appelle cette route et set user à null
- **MAIS ne redirige pas vers /login**

## 3. Composant modifié

**Fichier:** src/components/layout/Sidebar.tsx
**Sidebar/Header:** Sidebar (composant principal de navigation)

## 4. Implémentation

**Description précise du changement:**

1. **Import ajouté:** `import { useAuth } from '@omnikes/contexts/AuthContext'`
2. **Hook ajouté:** `const { logout } = useAuth()` dans le composant Sidebar
3. **Fonction handleLogout créée:**
   ```typescript
   const handleLogout = async () => {
     await logout();
     router.push('/login');
   };
   ```
4. **Footer modifié:**
   - Remplacé la zone footer qui affichait uniquement le bouton toggle en mode compact
   - Ajouté un bouton "Déconnexion" visible en mode normal (non-compact)
   - Le bouton affiche: icône 🚪 + texte "Déconnexion"
   - Hover: rouge (hover:bg-red-50 hover:text-red-600)
   - En mode compact: affiche toujours le bouton toggle (inchangé)
5. **Comportement:**
   - Clic sur "Déconnexion" → appelle AuthContext.logout() → redirige vers /login
   - Réutilise le mécanisme de logout existant
   - Ajoute la redirection manquante vers /login

## 5. Fichiers modifiés

**Liste exacte:**
- src/components/layout/Sidebar.tsx (+22 lignes, -6 lignes)

## 6. Validation

**TypeScript:** PASS
Commande: npx tsc --noEmit
Résultat: 0 erreur

**Build:** PASS
Commande: npm run build
Résultat: build réussi, TypeScript compilé, pages générées

**Test manuel:** NON EXÉCUTÉ
Application non démarrée pendant cette intervention

## 7. Sécurité

**RBAC modifié:** NON
**Permissions modifiées:** NON
**Auth core modifié:** NON
**Prisma modifié:** NON
**DB modifiée:** NON
**P0-14-A.4 modifié:** NON

Le bouton de déconnexion utilise uniquement:
- AuthContext.logout() existant
- Route /api/auth/logout existante
- Aucune modification des mécanismes d'authentification
- Aucune exposition de token
- Aucun contournement des contrôles RBAC

## 8. Git final

**Commit:** AUCUN
**Push:** AUCUN

État du working tree:
- M src/components/layout/Sidebar.tsx (P0-14-B.0.1)
- M src/repositories/role.repository.ts (P0-14-A.4)
- ?? rapports et diagnostics (non committés)
