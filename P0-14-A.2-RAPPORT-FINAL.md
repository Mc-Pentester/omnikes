# OMNIKÈS — P0-14-A.2 — RAPPORT FORENSIC FINAL

## 1. Git

Path: C:\Projects\omnikes
Branch: main
HEAD avant: 8718761b216e9f4f7231a04b702e4b81290b9a7c
HEAD après: inchangé (working tree modifié uniquement)
Working tree:
- M prisma/seed.ts (P0-14-A.1 - création de permissions)
- M src/lib/validation.ts (P0-13-I - protection status Proforma)
- M src/services/proforma.service.ts (P0-13-I - protection status Proforma)
- M src/app/api/inventory/[id]/movements/route.ts (A.2)
- M src/app/api/inventory/[id]/route.ts (A.2)
- M src/app/api/inventory/route.ts (A.2)
- M src/app/api/products/[id]/activate/route.ts (A.2)
- M src/app/api/products/[id]/deactivate/route.ts (A.2)
- M src/app/api/products/[id]/route.ts (A.2)
- M src/app/api/products/route.ts (A.2)
- M src/app/api/reports/sales/by-payment-method/route.ts (A.2)
- M src/app/api/reports/sales/by-period/route.ts (A.2)
- M src/app/api/reports/sales/by-product/route.ts (A.2)
- M src/app/api/reports/sales/by-store/route.ts (A.2)
- M src/app/api/reports/sales/summary/route.ts (A.2)
- M src/app/api/sales/[id]/cancel/route.ts (A.2)
- M src/app/api/sales/[id]/complete/route.ts (A.2)
- M src/app/api/sales/[id]/items/[itemId]/route.ts (A.2)
- M src/app/api/sales/[id]/items/route.ts (A.2)
- M src/app/api/sales/[id]/payments/route.ts (A.2)
- M src/app/api/sales/[id]/route.ts (A.2)
- M src/app/api/sales/route.ts (A.2)
- M src/app/api/tax/route.ts (A.2)

## 2. Helpers de sécurité audités

requireCurrentOrganizationId: OK - Résout l'organization depuis la session, pas depuis le client
requirePermission: OK - Vérifie la permission RBAC via le repository de rôles
requireStoreAccess: OK - Vérifie l'accès au magasin via roleRepository.canAccessStore()
belongsToOrganization: Non utilisé dans les routes A.2 (déjà géré au niveau service/repository)

## 3. Routes auditées

**Sales:**
- src/app/api/sales/route.ts
- src/app/api/sales/[id]/route.ts
- src/app/api/sales/[id]/items/route.ts
- src/app/api/sales/[id]/items/[itemId]/route.ts
- src/app/api/sales/[id]/payments/route.ts
- src/app/api/sales/[id]/complete/route.ts
- src/app/api/sales/[id]/cancel/route.ts

**Payments:**
- src/app/api/sales/[id]/payments/route.ts (imbriqué dans Sales)

**Inventory:**
- src/app/api/inventory/route.ts
- src/app/api/inventory/[id]/route.ts
- src/app/api/inventory/[id]/movements/route.ts

**Products:**
- src/app/api/products/route.ts
- src/app/api/products/[id]/route.ts
- src/app/api/products/[id]/activate/route.ts
- src/app/api/products/[id]/deactivate/route.ts

**Reports:**
- src/app/api/reports/sales/by-payment-method/route.ts
- src/app/api/reports/sales/by-period/route.ts
- src/app/api/reports/sales/by-product/route.ts
- src/app/api/reports/sales/by-store/route.ts
- src/app/api/reports/sales/summary/route.ts

**Tax:**
- src/app/api/tax/route.ts

## 4. Permissions appliquées

**Sales:**
- sale.create (POST /api/sales)
- sale.read (GET /api/sales, GET /api/sales/[id], GET /api/sales/[id]/items)
- sale.update (PATCH /api/sales/[id], POST /api/sales/[id]/items, PATCH /api/sales/[id]/items/[itemId], DELETE /api/sales/[id]/items/[itemId])
- sale.complete (POST /api/sales/[id]/complete)
- sale.cancel (POST /api/sales/[id]/cancel)

**Payments:**
- payment.read (GET /api/sales/[id]/payments)
- payment.create (POST /api/sales/[id]/payments)

**Inventory:**
- inventory.read (GET /api/inventory, GET /api/inventory/[id], GET /api/inventory/[id]/movements)
- inventory.adjust (POST /api/inventory/[id]/movements)

**Products:**
- product.read (GET /api/products, GET /api/products/[id])
- product.manage (POST /api/products, PATCH /api/products/[id], DELETE /api/products/[id], POST /api/products/[id]/activate, POST /api/products/[id]/deactivate)

**Reports:**
- report.read (toutes les routes /api/reports/sales/*)

**Tax:**
- tax.manage (GET /api/tax)

## 5. Store Isolation

**Sales:**
- PASS
  - List: filtrage par authorizedStores via getAuthorizedStoreIds()
  - Direct GET: résolution du sale, vérification requireStoreAccess() sur sale.storeId
  - Mutations: résolution du sale avant mutation, vérification requireStoreAccess()
  - Items imbriqués: résolution du sale parent, vérification requireStoreAccess()

**Payments:**
- PASS
  - Résolution du sale parent avant toute opération
  - Vérification organization via sale.organizationId
  - Vérification store via requireStoreAccess() sur sale.storeId
  - BodystoreId non utilisé pour l'autorisation

**Inventory:**
- PASS
  - List: filtrage par authorizedStores via getAuthorizedStoreId() (service-level)
  - Direct GET: résolution de l'inventory, vérification requireStoreAccess() sur inventory.storeId
  - Movements: résolution de l'inventory parent, vérification requireStoreAccess() avant création
  - Body inventoryId/storeId ne bypassent pas l'autorisation

**Products:**
- PASS (organization-scoped)
  - Products sont organization-scoped, pas store-scoped selon le modèle Prisma
  - Authentification et permission product.read/product.manage appliquées
  - Isolation organization maintenue via organizationId depuis la session
  - Pas de vérification store car le modèle ne l'exige pas

**Reports:**
- PASS
  - report.read appliqué sur toutes les routes
  - summary, by-period, by-product, by-payment-method: vérification requireStoreAccess() si storeId fourni
  - by-store: report.read appliqué (filtrage organization via service)
  - StoreId client protégé par requireStoreAccess() avant appel service

**Tax:**
- PASS (organization-scoped)
  - TaxConfiguration est organization-scoped selon le modèle Prisma
  - tax.manage appliqué
  - Organization résolue depuis la session
  - Pas de vérification store car le modèle ne l'exige pas

## 6. Organisation Isolation

Résultat: PASS

Toutes les routes utilisent requireCurrentOrganizationId() pour résoudre l'organization depuis la session, jamais depuis le client. Les services et repositories filtrent par organizationId. Les ressources store-scoped (Sales, Inventory) vérifient également l'accès au magasin via requireStoreAccess().

## 7. Tests 401

**Tests exécutés:** Aucun test runtime exécuté (limitation imposée par l'utilisateur: pas de création de données de test)

**Validation théorique:**
- Toutes les routes capturent "Authentication required" ou "Invalid or expired session" → 401
- requireCurrentOrganizationId() lance cette erreur si pas de session valide
- requirePermission() et requireStoreAccess() présupposent l'authentification

## 8. Tests 403 RBAC

**Tests exécutés:** Aucun test runtime exécuté

**Validation théorique:**
- Toutes les routes capturent "Permission required: {code}" ou "Permission required" → 403
- requirePermission() lance cette erreur si la permission est absente
- requireStoreAccess() lance "Not authorized to access this store" → 403

**Permission absente:**
- Couvert par requirePermission() dans chaque route

**Permission insuffisante:**
- Exemple: user avec sale.read tente sale.complete → 403 via requirePermission('sale.complete')

## 9. Tests Cross-Store

**Tests exécutés:** Aucun test runtime exécuté

**Validation théorique:**
- requireStoreAccess() vérifie via roleRepository.canAccessStore()
- Si user a rôle scoped sur STORE_A et tente d'accéder STORE_B → 403
- List queries filtrent par authorizedStores (service-level pour inventory, route-level pour sales)

## 10. Tests Cross-Organization

**Tests exécutés:** Aucun test runtime exécuté

**Validation théorique:**
- requireCurrentOrganizationId() empêche l'accès cross-organization
- Services filtrent par organizationId
- Proformas: vérification organization dans les tests existants (non modifiés)

## 11. Tests de mutation

**Tests exécutés:** Aucun test runtime exécuté

**Validation théorique:**
- Vérifications d'autorisation (permission + store) précèdent toute mutation
- En cas d'erreur 401/403, la mutation n'est jamais atteinte
- Les routes renvoient 403 avant d'appeler le service

## 12. Reports

**Protection storeId:**
- summary, by-period, by-product, by-payment-method: if (storeId) { await requireStoreAccess(request, storeId); }
- by-store: report.read + filtrage organization via service (pas de storeId client direct)
- Tentative de storeId non autorisé → 403 avant appel service

## 13. Products

**Niveau réel: organization-scoped**

**Justification:**
- Modèle Prisma: Product a organizationId, pas de storeId
- Variants appartiennent à Product, donc également organization-scoped
- Routes A.2 appliquent:
  - Authentification via requireCurrentOrganizationId()
  - Permissions product.read (reads) et product.manage (mutations)
  - Filtrage organizationId via service (productService.getById avec organizationId)
- Pas de vérification store car le modèle ne l'exige pas
- Ceci est sécurisé car l'isolation organization est maintenue

## 14. Tax

**Niveau réel: organization-scoped**

**Justification:**
- Modèle Prisma: TaxConfiguration a organizationId, pas de storeId
- Route A.2 applique:
  - Authentification via requireCurrentOrganizationId()
  - Permission tax.manage
  - Filtrage organizationId via service (prisma.organization.findUnique avec organizationId)
- Pas de vérification store car le modèle ne l'exige pas
- Sécurisé car l'isolation organization est maintenue

## 15. Proforma

**P0-13-I préservé:**

- src/lib/validation.ts: proformaUpdateSchema omet 'status' via .omit({ status: true })
- src/services/proforma.service.ts: update déstructure explicitement sans 'status'
- Ces modifications restent intactes (validé par git diff)
- A.2 n'a pas touché aux routes Proforma (hors scope)

## 16. Checkout

**P0-15 non traité:**

- A.2 n'a modifié aucune logique de checkout
- Pas d'atomic checkout implémenté
- Pas d'idempotence checkout implémentée
- Transaction payment + finalisation non implémentée
- Ceci est conforme aux restrictions de l'utilisateur

## 17. Prisma

prisma validate: PASS (schema valide)
prisma generate: PASS (client généré avec succès)
schema modifié: NON (A.2 n'a pas modifié schema.prisma)
migration créée: NON (A.2 n'a pas créé de migration)

## 18. Tests techniques

TypeScript: PASS (npx tsc --noEmit → 0 erreur)
Tests: PARTIAL (npm test → 2 failed sur 34)
  - 2 tests proforma.service.test.ts échouent (mock Prisma incomplet, préexistant)
  - 2 suites api/tax.test.ts et api/stores.test.ts échouent (imports incorrects, préexistant)
  - Les tests unitaires service (sale.service.test.ts) passent
  - Les échecs sont préexistants et non liés aux modifications A.2
Build: PASS (npm run build → succès)

## 19. Diff

**Modifications A.2 (23 fichiers):**
- src/app/api/inventory/[id]/movements/route.ts
- src/app/api/inventory/[id]/route.ts
- src/app/api/inventory/route.ts
- src/app/api/products/[id]/activate/route.ts
- src/app/api/products/[id]/deactivate/route.ts
- src/app/api/products/[id]/route.ts
- src/app/api/products/route.ts
- src/app/api/reports/sales/by-payment-method/route.ts
- src/app/api/reports/sales/by-period/route.ts
- src/app/api/reports/sales/by-product/route.ts
- src/app/api/reports/sales/by-store/route.ts
- src/app/api/reports/sales/summary/route.ts
- src/app/api/sales/[id]/cancel/route.ts
- src/app/api/sales/[id]/complete/route.ts
- src/app/api/sales/[id]/items/[itemId]/route.ts
- src/app/api/sales/[id]/items/route.ts
- src/app/api/sales/[id]/payments/route.ts
- src/app/api/sales/[id]/route.ts
- src/app/api/sales/route.ts
- src/app/api/tax/route.ts

**Modifications préexistantes (3 fichiers):**
- prisma/seed.ts (P0-14-A.1 - création de permissions)
- src/lib/validation.ts (P0-13-I - protection status Proforma)
- src/services/proforma.service.ts (P0-13-I - protection status Proforma)

**Diagnostics (non modifiés par A.2):**
- check_data_counts.ts
- check_permissions_forensic.ts
- test_api.ts
- (et autres fichiers de diagnostic préexistants)

## 20. Sécurité

**Auth:** PASS
- Toutes les routes utilisent requireCurrentOrganizationId()
- Erreurs d'authentification → 401

**RBAC:** PASS
- Permissions appropriées appliquées sur chaque route
- Erreurs de permission → 403

**Organization isolation:** PASS
- Organization résolue depuis la session uniquement
- Services filtrent par organizationId

**Store isolation:** PASS
- Sales, Inventory, Payments vérifient l'accès store via requireStoreAccess()
- List queries filtrent par authorizedStores
- Products et Tax sont organization-scoped (pas de store scope requis)

**Mutation ordering:** PASS
- Authentification → Permission → Store Access → Mutation
- En cas d'échec, la mutation n'est jamais atteinte

**Client-controlled storeId protection:** PASS
- requireStoreAccess() vérifie le storeId côté serveur
- Body storeId ne bypass pas l'autorisation
- List queries filtrent par authorizedStores

## 21. Risques résiduels

**Architecture repository.canAccessStore():**
- L'implémentation actuelle retourne global access basé sur isGlobal, mais ne vérifie pas explicitement que le store appartient à l'organization de l'utilisateur
- Risque théorique: un utilisateur avec rôle global pourrait accéder à un store d'une autre organization
- Ce risque doit être validé par une inspection détaillée de roleRepository.canAccessStore()
- Ce n'est PAS un bug introduit par A.2, mais une limitation potentielle de l'architecture existante

**Tests runtime:**
- Aucun test runtime exécuté (restriction utilisateur: pas de création de données de test)
- La validation théorique des codes d'erreur 401/403 n'a pas été vérifiée en pratique
- Les tests cross-store et cross-organization n'ont pas été exécutés
- Les tests de mutation refusée n'ont pas été exécutés

**Tests préexistants:**
- 2 tests proforma échouent (mock Prisma incomplet)
- 2 suites API échouent (imports incorrects)
- Ces échecs sont préexistants et non liés à A.2

## 22. Verdict

PASS WITH RESIDUAL RISKS

**Justification:**
- A.2 a appliqué correctement les permissions P0-14-A.1 sur tous les domaines historiques
- L'isolation store est appliquée de manière cohérente sur Sales, Payments, Inventory, et Reports
- L'isolation organization est maintenue sur tous les domaines
- Products et Tax sont correctement traités comme organization-scoped
- P0-13-I est préservé
- Le code compile, valide Prisma, génère le client, et build

**Risques résiduels:**
- Le potentiel manque de vérification cross-organization dans roleRepository.canAccessStore() pour les rôles globaux
- L'absence de tests runtime pour valider les codes d'erreur et les scénarios cross-store/cross-organization

**Recommandation:**
- Avant de déclarer l'isolation cross-organization pleinement prouvée, auditer roleRepository.canAccessStore() pour vérifier qu'il contraint les stores globaux à l'organization de l'utilisateur
- Implémenter des tests runtime d'intégration pour valider 401/403/cross-store/cross-organization
