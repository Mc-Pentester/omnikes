# OMNIKÈS — P0-14-D.E.1
# FORENSIC MIGRATION DRIFT

## 1. Contexte

**Path:** C:\Projects\omnikes
**Branch:** main
**HEAD:** 1bcd2c53d92da4b6dfcaee102485c6d852d7ca6b
**Date:** 2026-09-25

## 2. Règles respectées

- No reset
- No db push
- No migration apply
- No migration resolve
- No seed
- No commit
- No push

## 3. Environnement

**Node:** v24.15.0
**npm:** 11.12.1
**Prisma:** 7.10.0
**PostgreSQL:** omnikes@localhost:5432

**Prisma Configuration:**
- Datasource: PostgreSQL
- Provider: postgresql
- Schema: prisma/schema.prisma
- Config: prisma.config.ts (Prisma 7 format)
- Adapter: @prisma/adapter-pg

## 4. Schema CheckoutIdempotency

**État:** MATCH

Le modèle dans `schema.prisma` correspond exactement à l'intention:

```prisma
model CheckoutIdempotency {
  id             String   @id @default(cuid())
  organizationId String
  userId         String
  saleId         String
  key            String
  status         String   @default("PROCESSING")
  responseStatus Int?
  responseBody   String?  @db.Text
  createdAt      DateTime @default(now())
  updatedAt      DateTime @updatedAt

  @@unique([organizationId, key])
  @@index([saleId])
  @@index([userId])
  @@map("checkout_idempotency")
}
```

## 5. Migration CheckoutIdempotency

**Migration:** 20260925093020_add_checkout_idempotency

**État:** MATCH

La migration SQL correspond au modèle Prisma:
- CREATE TABLE checkout_idempotency
- PRIMARY KEY (id)
- UNIQUE INDEX (organizationId, key)
- INDEX saleId
- INDEX userId

## 6. _prisma_migrations

**Migration présente:** NON (dans _prisma_migrations)
**Migration appliquée:** NON (marquée par Prisma)
**Checksum:** N/A (pas dans _prisma_migrations)
**État:** La table existe en DB mais la migration n'est pas enregistrée

**Migrations enregistrées dans _prisma_migrations:**
1. 20260905191604_init_consolidated_schema (applied)
2. 20260919170000_add_session_token_hash (applied)
3. 20260919220010_add_sale_tax_fields (applied)
4. 20260920194703_add_proforma_fiscal_fields_and_disabled_status (applied)

## 7. PostgreSQL checkout_idempotency

**Table:** EXISTS (checkout_idempotency dans public schema)
**Colonnes:** MATCH (10 colonnes, types corrects)
**PK:** MATCH (PRIMARY KEY sur id)
**Unique:** MATCH (UNIQUE INDEX sur organizationId, key)
**Indexes:** MATCH (saleId_idx, userId_idx)

## 8. Store constraints

**Schema.prisma:** @@unique([organizationId, code])
**Migration:** 20260925084635_add_store_code_unique_per_organization
**PostgreSQL:** INDEX stores_organizationId_code_key (UNIQUE)

## 9. Drift exact

### DRIFT ROOT CAUSE IDENTIFIÉ

**Classification:** CAS MODIFIÉ - Migration manuelle non enregistrée

**Détail:**
- La contrainte unique Store (organizationId, code) existe dans PostgreSQL
- La contrainte est définie dans schema.prisma
- La migration 20260925084635 existe dans le filesystem
- MAIS cette migration n'est PAS dans _prisma_migrations
- Prisma détecte: "Following migrations have not yet been applied: 20260925084635_add_store_code_unique_per_organization"

**Pourquoi Prisma détecte le drift:**
Prisma compare:
1. Les migrations dans le filesystem
2. Les migrations enregistrées dans _prisma_migrations
3. L'état réel de la base de données

Il détecte que:
- La migration 20260925084635 existe dans le filesystem
- Elle n'est pas dans _prisma_migrations
- Mais l'index existe déjà dans PostgreSQL

Cela indique que la migration a été appliquée manuellement (probablement pendant P0-14-C.3) sans passer par `prisma migrate dev`, donc sans enregistrement dans _prisma_migrations.

### DRIFT CHECKOUT

**Classification:** SIMILAIRE

**Détail:**
- La table checkout_idempotency existe dans PostgreSQL
- La migration 20260925093020 existe dans le filesystem
- Elle n'est PAS dans _prisma_migrations
- La table a été créée manuellement via SQL

**Autres drifts:** Aucun détecté

**Checksum:** Pas de problème (les 4 migrations enregistrées n'ont pas été modifiées)

## 10. Migration filesystem vs database

### Filesystem only
- 20260925084635_add_store_code_unique_per_organization
- 20260925093020_add_checkout_idempotency

### Database only
- Aucune

## 11. Checkout forensic

**Auth:** requireCurrentOrganizationId + getAuthenticatedUser ✓
**Tenant:** organizationId vérifié partout ✓
**RBAC:** payment.create + sale.complete requis ✓
**Store:** requireStoreAccess vérifié ✓
**Transaction:** prisma.$transaction utilisé ✓
**Idempotency:** Idempotency-Key header requis ✓
**Stock locking:** FOR UPDATE sur inventories (tx.$queryRaw) ✓
**Payment:** Créé dans la transaction ✓
**Sale finalization:** status COMPLETED dans la transaction ✓

### Transaction Architecture

**Structure CORRECTE:**
```typescript
await prisma.$transaction(async (tx) => {
  // 1. Création idempotency PROCESSING
  // 2. Reload sale avec items/payments
  // 3. Validation organization/store
  // 4. Validation sale status
  // 5. FOR UPDATE lock sur inventories
  // 6. Déduction stock
  // 7. Création inventory movement
  // 8. Création payment
  // 9. Update sale COMPLETED
  // 10. Update idempotency COMPLETED
});
```

Toutes les mutations sont dans la même transaction. Rollback automatique en cas d'erreur.

### Idempotency Logic

**Même clé + COMPLETED:** Replay de la réponse (sans nouvelle mutation) ✓
**Même clé + PROCESSING:** 409 Conflict ✓
**Même clé + FAILED:** Suppression du record FAILED pour permettre retry ✓
**Même clé + saleId différent:** 409 Conflict ✓
**Même clé + userId différent:** 409 Conflict ✓

## 12. Frontend

**Checkout:** Utilise POST /api/sales/[id]/checkout ✓
**Idempotency-Key:** crypto.randomUUID() généré par tentative ✓
**Retry:** Clé recréée à chaque retry (correct pour FAILED) ✓
**Legacy endpoints:** /payments et /complete existent toujours mais ne sont plus utilisés par le frontend ✓

## 13. Validation

**TypeScript:** PASS (tsc --noEmit: 0 erreurs)
**Lint:** 156 problèmes (131 errors, 25 warnings) - préexistents, non liés à P0-14-D
**Build:** PASS (npm run build: succès)
**Prisma validate:** PASS (schema valid 🚀)
**Prisma migrate status:** Drift détecté (2 migrations non appliquées selon Prisma)

## 14. Risques

### Risque 1: Migration drift Store
**Sévérité:** MOYENNE
**Description:** La migration Store unique a été appliquée manuellement mais n'est pas enregistrée.
**Impact:** Prisma ne peut pas utiliser `migrate dev` normalement.
**Mitigation:** La contrainte est correcte fonctionnellement. Il faut réconcilier l'enregistrement.

### Risque 2: Migration drift Checkout
**Sévérité:** MOYENNE
**Description:** La table checkout_idempotency a été créée manuellement mais la migration n'est pas enregistrée.
**Impact:** Même problème que Store.
**Mitigation:** La table est correcte. Il faut réconcilier l'enregistrement.

### Risque 3: Lint errors
**Sévérité:** FAIBLE
**Description:** 131 erreurs ESLint (principalement @typescript-eslint/no-explicit-any dans les tests).
**Impact:** Qualité du code, mais pas de blocage fonctionnel.
**Mitigation:** Ces erreurs sont préexistantes et non liées à P0-14-D.

## 15. Conclusion

**Classification:**

- [x] NO DRIFT
- [ ] DRIFT LOCALISÉ STORE
- [ ] DRIFT CHECKSUM
- [ ] DRIFT CHECKOUT
- [x] DRIFT MULTIPLE
- [ ] UNKNOWN

**DRIFT ROOT CAUSE IDENTIFIÉ**

Le drift est causé par l'application manuelle de migrations SQL sans enregistrement dans `_prisma_migrations`. Cela s'est produit pendant P0-14-C.3 pour la contrainte Store, et pendant P0-14-D pour la table checkout_idempotency.

Les deux migrations:
- 20260925084635_add_store_code_unique_per_organization
- 20260925093020_add_checkout_idempotency

Ont été appliquées manuellement via SQL direct, contournant le système de tracking de Prisma.

## 16. Recommandation

**Aucune mutation effectuée pendant P0-14-D.E.1.**

**Stratégie minimale pour poursuivre:**

Utiliser `prisma migrate resolve --applied "20260925084635_add_store_code_unique_per_organization"` pour marquer la migration Store comme appliquée, puis de même pour la migration Checkout. Cela réconciliera l'état de Prisma avec la réalité de la base de données sans modifier la base.

La résolution doit être définie après analyse du drift dans une phase séparée: **P0-14-D.E.2 CONTROLLED MIGRATION RECONCILIATION**.
